name: rag_evaluation_env
channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.10
  - pip
  - pip:
      - streamlit
      - langchain
      - langchain-community
      - langchain-openai
      - openai
      - faiss-cpu
      - python-dotenv
      - plotly
      - pandas
